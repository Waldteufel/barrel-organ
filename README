run with

   cargo run <roll1.txt | paplay --rate=11025 --format=float32le --channels=1 --raw

or

   cargo run <roll1.txt | aplay --rate=11025 --channels=1 -f FLOAT_LE

(there is a C version in branch 'legacy')
